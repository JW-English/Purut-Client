# 관리자 웹 환경변수.
# NEXT_PUBLIC_ 접두사는 브라우저 번들에 포함되므로 비밀 값은 넣지 않는다.

# 로컬 서버. 비워두면 http://localhost:8080 을 쓴다.
NEXT_PUBLIC_API_URL=http://localhost:8080

# 주의: Refresh 쿠키는 SameSite=Strict 다.
# 운영에서는 관리자 웹과 API 가 같은 사이트여야 쿠키가 실린다.
# (예: admin.purutpurut.com / api.purutpurut.com)
